'use strict';

/**
 * alternative-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::alternative-page.alternative-page');
